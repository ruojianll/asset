angular.module('asset').controller('menu',function($scope,$state){
	if(boolean){
		$state.go("signIn.assets");
	}
		// $(window).load(function(){
		// 	alert(1)
		// 	var url = window.location.href;
		// 	var boolean = false;
		// 	var now = url.split('-')[1]
		// 	for(var i=0;i<$('.uib-tab').length;i++){
		// 		var a = $('.uib-tab').eq(i).attr('heading')
		// 		if(a == now){
		// 			$('.uib-tab').eq(i).addClass('active').siblings().removeClass('active')
		// 		}
		// 	}
		// })
		// debugger;
		// $scope.currentPage = 1;
}) 
